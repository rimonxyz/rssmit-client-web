import {Promo} from "./promo.model";

export class PromosPage {
  content: Promo[];
  last: boolean;
  totalPages: number;
  totalElements: number;
  sort: any[];
  first: boolean;
  numberOfElements: number;
  size: number;
  number: number;
}
