import {RShared} from "./rshared.model";

export class RSharedPage {
  content: RShared[];
  last: boolean;
  totalPages: number;
  totalElements: number;
  sort: any[];
  first: boolean;
  numberOfElements: number;
  size: number;
  number: number;
}
